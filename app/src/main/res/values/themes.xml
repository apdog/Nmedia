<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NMedia" parent="Theme.Material3.DayNight.NoActionBar">

    </style>

    <style name="Theme.NMedia" parent="Base.Theme.NMedia" />

<!--стили для кнопок в postItem-->

    <style name="Theme.LikesButton" parent="Widget.Material3.Button.TextButton.Icon">
        <item name="iconTint">@color/like_color</item>
        <item name="android:minWidth">20dp</item>
        <item name="android:minHeight">20dp</item>
    </style>

    <style name="Theme.CommentsButton" parent="Widget.Material3.Button.TextButton.Icon">
        <item name="iconTint">@color/grey</item>
        <item name="android:minWidth">20dp</item>
        <item name="android:minHeight">20dp</item>
    </style>

    <style name="Theme.SharesButton" parent="Widget.Material3.Button.TextButton.Icon">
        <item name="iconTint">@color/grey</item>
        <item name="android:minWidth">20dp</item>
        <item name="android:minHeight">20dp</item>
    </style>

    <style name="Theme.ViewsButton" parent="Widget.Material3.Button.TextButton.Icon">
        <item name="iconTint">@color/grey</item>
        <item name="android:minWidth">20dp</item>
        <item name="android:minHeight">20dp</item>
    </style>

    <style name="Theme.OptionMenuButton" parent="Widget.Material3.Button.TextButton.Icon">
        <item name="iconTint">@color/grey</item>
        <item name="android:minWidth">20dp</item>
        <item name="android:minHeight">20dp</item>
    </style>

    <style name="Theme.NMedia.Transparent" parent="Base.Theme.NMedia">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>